# context-notes.md — 탱크 이동 애니메이션

## 2026-05-21

### 스프라이트 규격
- `default_tank_up_6.png`: 1536×1024px, 3열×2행, 프레임 1개 = 512×512
- 프레임 배치: 좌상(0,0), 중상(512,0), 우상(1024,0), 좌하(0,512), 중하(512,512), 우하(1024,512)

### 회전 오프셋 결정
- 스프라이트가 UP 방향을 향함 → Godot에서 UP = angle() = -PI/2
- 이동 방향 각도에 +PI/2 더해야 스프라이트가 이동 방향과 일치
- 수식: `rotation = direction.angle() + PI / 2`

### 스케일 결정
- 원본 크기 512×512, 게임 플레이어는 약 64×64px 수준
- `Vector2(0.15, 0.15)` → 렌더 크기 약 76×76px (플레이어보다 살짝 큰 탱크)

### last_direction 도입 이유
- 정지 시 direction = Vector2.ZERO라 rotation 계산 불가
- 마지막 이동 방향을 저장해 정지 시에도 자세 유지

### attack 애니메이션 제거
- 탱크 스프라이트에 attack 프레임 없음, 코드에서도 미사용 → 제거
